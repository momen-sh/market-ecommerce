import { Route } from '@angular/router';
import { ProductDetailComponent } from '../components';

export const PRODUCT_DETAIL_ROUTE: Route = { path: 'product/:id', component: ProductDetailComponent };

